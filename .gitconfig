[alias]
	ch = checkout
	cm = commit
	a = add
	br = branch
	st = status
	p = push
	rb = rebase
	hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
	rbr = for-each-ref --sort=-committerdate --count=10 --format='%(refname:short)' refs/heads/

